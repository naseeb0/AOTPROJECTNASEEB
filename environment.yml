name: aiot
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pytorch>=2.0.0
  - torchvision>=0.15.0
  - pytorch-cuda=11.8  # For GPU support on Azure
  - numpy>=1.21.0
  - matplotlib>=3.5.0
  - pillow>=9.0.0
  - tqdm>=4.64.0
  - pip
  - pip:
    - tensorboard>=2.10.0
